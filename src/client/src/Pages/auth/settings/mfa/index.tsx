import { PageLayout } from '../../../../Components';
import { sidebarLinks } from '../links';

const MFA = () => {
  return (
    <PageLayout
      title="MFA"
      heading="Multi Factor Authentication"
      hideResourses
      links={sidebarLinks}
    >
      MFA
    </PageLayout>
  );
};

export default MFA;
